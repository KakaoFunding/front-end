import React from 'react';

import styles from './index.module.scss';

const SearchResult: React.FC = () => {
  return <section className={styles.area_search_result}>{/* 탭 목록, 선택한 탭에 따른 컨텐츠 */}</section>;
};

export default SearchResult;
