import styles from './index.module.scss';

const CartPay = () => {
  return (
    <section className={styles.area_cartpay}>
      <p className={styles.txt_cartpay}>
        선물상자에 담은 상품은 최대 30일간 보관됩니다.
      </p>
    </section>
  );
};

export default CartPay;
