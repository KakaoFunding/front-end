import styles from './index.module.scss';

const GiftReview = () => {
  return (
    <section className={styles.wrapper_gift_review}>
      준비중인 기능입니다.
    </section>
  );
};

export default GiftReview;
